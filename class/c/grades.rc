#!/usr/bin/perl

use strict;
use warnings;
use IO::All;
use YAML qw/LoadFile DumpFile/;

use Grades;
my $l = League->new( id => '.' );;
my $g = Grades->new( league => $l );;
my %m = map { $_->{id} => $_ } @{ $l->members };
